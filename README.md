# Git & GitHub Learning 📚

A repository documenting my journey of learning Git and GitHub — version control fundamentals, commands, and workflows, practiced hands-on as I build other projects alongside this.

## 🎯 Purpose

This repo is my practice space and notes while learning:
- Git basics (init, add, commit, status, log)
- Working with `.gitignore`
- Connecting local repos to GitHub (remote, push, pull, clone)
- Branching and merging
- Resolving basic merge conflicts

## 📖 What I've Learned So Far

| Concept | Status |
|---|---|
| git init / add / commit | ✅ |
| git status / log / diff | ✅ |
| .gitignore | 🔄 |
| git remote / push / pull | 🔄 |
| git clone | — |
| Branching (checkout -b, merge) | — |
| Merge conflicts | — |

## 🛠️ Common Commands Cheat Sheet

```bash
git init                     # start a new repo
git status                   # check current changes
git add <file>                # stage changes
git commit -m "message"       # save a snapshot
git log                        # view commit history
git remote add origin <url>    # link to GitHub repo
git push origin main            # push changes to GitHub
git pull origin main             # fetch + merge latest changes
git branch <name>                 # create a new branch
git checkout -b <name>             # create + switch to new branch
git merge <branch>                  # merge a branch into current one
```

## 📌 Notes

This is a living document — I'll keep updating it as I learn more (branching strategies, rebasing, pull requests, etc.)

---

⭐ Learning in public — feel free to follow along!
